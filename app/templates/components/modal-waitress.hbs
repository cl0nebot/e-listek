{{yield (action (perform requestWaitress)) isDisabled}}

{{#if isModalOpen}}
  {{#modal-dialog
      targetAttachment="center"
      translucentOverlay=true}}
    <h1 class="exclusive waitress">Obsluha je na ceste</h1>
  {{/modal-dialog}}
{{/if}}
