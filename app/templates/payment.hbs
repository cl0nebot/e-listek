<h2>K zaplacení</h2>

{{#each model as |ticket|}}
  <div>{{ticket.name}}</div>
  <ul>
    {{#each ticket.items as |item|}}
      <li>{{item.name}}</li>
    {{/each}}
  </ul>
  <button {{action 'pay' ticket}}>Zaplatit</button>
{{/each}}

{{#if isPayModalVisible}}
  {{modal-pay model=ticket}}
{{/if}}
